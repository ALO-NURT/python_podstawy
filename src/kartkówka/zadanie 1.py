def zadanie1 (lista_liczb):
    for liczba in lista_liczb:
        print(liczba)
    lista_liczb = ([1,0,-2,5,7,8])
    for liczba in lista_liczb
        if:
            liczba < 0 = x
            print(ilosc(x))
        if:
            liczba % 2 == 1
                if liczba min(lista_liczb)
                print(liczba)




        